Iniciando projeto: 
+ npm init -y 

Instalando express:
+ npm install express

Instalando nodemon:
+ npm install -g nodemon ou 
+ npm install --save-dev nodemon

Instalando  yup:
+ npm install --save yup

Subindo o servidor:
+ npm start
